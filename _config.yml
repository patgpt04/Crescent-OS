title: "Crescent OS"
description: "Personal operations system"
theme: minima
markdown: kramdown

# Use clean URLs (no .html)
permalink: pretty

# Minima header navigation (ONLY what you list here appears in the top bar)
header_pages:
  - systems/task-console.md

# Optional: keep your old nav list for future custom themes/layouts
# (Minima ignores this, but leaving it doesn't hurt if you later switch themes)
nav:
  - title: "Task Console"
    url: /systems/task-console/
